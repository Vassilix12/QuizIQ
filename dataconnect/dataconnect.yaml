specVersion: "v1"
serviceId: "quiziq"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quiziq-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
